on:
    issues:
    workflow_dispatch:

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Print github context
              run: echo "${{ toJSON(github) }}"

            